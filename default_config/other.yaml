# 白名单如果存在，则黑名单会无效，白名单的格式罩着黑名单加即可
# 黑名单是禁用命令和邮件指令中包含关键词的指令
# 白名单是只允许存在白名单的关键词的指令通过
# 比如白名单设置item，则只有item对应的指令可以通过
# 如果需要命令启用白名单，而邮件不想启用白名单，则在白名单中添加“邮件”即可，让邮件通过白名单


blacklist: # 黑名单
  - 23334
  - item add all

whitelist: # 白名单